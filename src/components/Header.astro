---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
---

<header>
	<nav>
		<a href="/" class="brand">{SITE_TITLE}<span class="dot">.</span></a>
		<div class="internal-links">
			<HeaderLink href="/">Inicio</HeaderLink>
			<HeaderLink href="/cursos">Cursos</HeaderLink>
			<HeaderLink href="/plantillas">Plantillas</HeaderLink>
			<HeaderLink href="/software">Software</HeaderLink>
			<HeaderLink href="/ebooks">Ebooks</HeaderLink>
			<HeaderLink href="/sobre">Sobre</HeaderLink>
		</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1.5em;
		background: var(--navy-900);
		border-bottom: 1px solid var(--navy-700);
		position: sticky;
		top: 0;
		z-index: 20;
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		max-width: 1100px;
		margin: 0 auto;
		flex-wrap: wrap;
	}
	.brand {
		display: flex;
		align-items: baseline;
		text-decoration: none;
		padding: 1em 0;
		font-family: var(--font-display, Georgia, serif);
		font-style: italic;
		font-weight: 600;
		font-size: 1.5em;
		letter-spacing: -0.01em;
		color: var(--sky);
	}
	.dot {
		color: var(--brass-bright);
		font-style: normal;
	}
	.internal-links {
		display: flex;
		align-items: center;
		gap: 0.15em;
	}
	.internal-links :global(a) {
		padding: 1em 0.6em;
		color: var(--mist);
		text-decoration: none;
		font-size: 0.9em;
		position: relative;
	}
	.internal-links :global(a:hover) {
		color: var(--brass-bright);
	}
	.internal-links :global(a.active) {
		text-decoration: none;
		color: var(--sky);
	}
	.internal-links :global(a.active)::after {
		content: '';
		position: absolute;
		left: 0.6em;
		right: 0.6em;
		bottom: 0.85em;
		height: 1px;
		background: var(--brass);
	}
</style>
